# Dev-op - From zero to the working one!

## How to start from nothing.
1.) Check if there is GIT installed by using the command :
```bash
git version
```
2.) Install Docker.
```bash
sudo apt install docker.io
docker --version
```
